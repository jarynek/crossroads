import { ToggleSectionDirective } from './toggle-section.directive';

describe('ToggleSectionDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleSectionDirective();
    expect(directive).toBeTruthy();
  });
});
