import { SortBySectionPipe } from './sort-by-section.pipe';

describe('SortBySectionPipe', () => {
  it('create an instance', () => {
    const pipe = new SortBySectionPipe();
    expect(pipe).toBeTruthy();
  });
});
